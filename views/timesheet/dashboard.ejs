
<!DOCTYPE html>
<html>
<head>
  <title>Dashboard</title>
  <style>
    body { font-family: Arial, sans-serif; padding: 20px; }
    h2 { margin-bottom: 20px; }
    .cards {
      display: flex;
      gap: 20px;
      margin-bottom: 30px;
    }
    .card {
      cursor: pointer;
      border: 1px solid #ccc;
      border-radius: 8px;
      padding: 30px 50px;
      box-shadow: 0 4px 6px rgba(0,0,0,0.1);
      transition: box-shadow 0.2s ease;
      text-align: center;
      font-size: 18px;
      font-weight: bold;
      background: #f9f9f9;
      user-select: none;
    }
    .card:hover {
      box-shadow: 0 8px 12px rgba(0,0,0,0.2);
      background: #e0f7fa;
    }
  </style>
</head>
<body>
  <h2>Welcome, <%= user %></h2>
  <a href="/logout">Logout</a>

  <div class="cards">
    <div class="card" onclick="location.href='/mybucket'">MyBucket Search</div>
    <div class="card" onclick="location.href='/timesheet'">Timesheet</div>
    <div class="card" onclick="location.href='/backendtool'">BackendTool</div>
  </div>

  <h3>Upload File to AWS S3</h3>
  <form action="/upload" method="POST" enctype="multipart/form-data">
    <input type="file" name="file" required />
    <button type="submit">Upload</button>
  </form>
</body>
</html>

